apiVersion: v1
kind: Service

metadata:
  name: postgres-service
  namespace: note
  labels:
    app: postgres

spec:
  selector:
    app: postgres
  
  ports:
    - name: http
      port: 5432
      targetPort: 5432

  type: ClusterIP
  