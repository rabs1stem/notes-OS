apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: redis-pvc
  namespace: note

spec:
  storageClassName: local-path
  
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 3Gi